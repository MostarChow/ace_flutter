import 'package:flutter/material.dart';

class MCColors {
  // 主色调
  static const primary_color = Color.fromRGBO(51, 51, 51, 1);

  // 背景
  static const background_color = Color.fromRGBO(245, 245, 245, 1);

  // 分割线
  static const line_color = Color.fromRGBO(204, 204, 204, 1);

  // 红色金额
  static const money_color = Color.fromRGBO(233, 0, 0, 1);
}